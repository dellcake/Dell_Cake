# گزارش تحلیل مهاجرت از Firebase به Supabase - وب‌سایت دل‌کیک

این گزارش شامل تحلیل فنی معماری فعلی، مشکلات آن و نقشه راه پیشنهادی برای مهاجرت به معماری جدید جهت دسترسی بدون محدودیت کاربران داخل ایران می‌باشد.

## ۱. معماری فعلی (Current Architecture)

*   **Frontend:** میزبانی شده روی GitHub Pages (Static HTML/JS).
*   **Authentication:** استفاده از Firebase Auth (Google & Email/Password).
*   **Database:** استفاده از Firebase Firestore (NoSQL).
*   **Storage:** استفاده از Firebase Storage برای تصاویر کیک‌ها و گالری.
*   **Logic:** منطق برنامه به صورت Client-side در فایل‌های JavaScript اجرا می‌شود.

### مشکلات معماری فعلی:
1.  **تحریم و فیلترینگ:** سرویس‌های Firebase (به ویژه Auth و Firestore) برای IPهای ایران مسدود هستند، که باعث می‌شود کاربران بدون VPN نتوانند وارد شوند یا سفارشی ثبت کنند.
2.  **NoSQL Limitations:** با پیچیده‌تر شدن سفارشات و دوره‌ها، استفاده از یک پایگاه داده رابطه‌ای (Relational) مانند PostgreSQL کارآمدتر است.

---

## ۲. فایل‌های وابسته به Firebase

فایل‌های زیر مستقیماً با سرویس‌های Firebase در تعامل هستند و نیاز به تغییر یا جایگزینی دارند:

| ردیف | مسیر فایل | نقش در سیستم |
| :--- | :--- | :--- |
| ۱ | `js/firebase-config.js` | تنظیمات اصلی Firebase |
| ۲ | `js/firebase-auth.js` | توابع کمکی احراز هویت |
| ۳ | `js/firebase-db.js` | توابع کمکی دیتابیس (Firestore) |
| ۴ | `js/firebase-storage.js` | توابع کمکی مدیریت فایل |
| ۵ | `js/user-auth-state.js` | مدیریت وضعیت لاگین در کل سایت |
| ۶ | `js/site-settings.js` | دریافت تنظیمات سایت از Firestore |
| ۷ | `js/academy-popup.js` | دریافت لیست دوره‌ها |
| ۸ | `admin/js/login.js` | لاگین مدیر |
| ۹ | `admin/js/admin.js` | مدیریت محتوا، سفارشات و گالری |
| ۱۰ | `admin/js/auth-guard.js` | محافظت از مسیرهای پنل مدیریت |
| ۱۱ | `user/js/auth.js` | ثبت‌نام و ورود کاربران |
| ۱۲ | `user/js/dashboard.js` | داشبورد کاربری |

---

## ۳. معماری پیشنهادی (Target Architecture)

برای حل مشکل دسترسی کاربران ایران، معماری زیر پیشنهاد می‌شود:

*   **Frontend:** حفظ روی GitHub Pages (بدون تغییر در میزبانی).
*   **Backend & DB:** استفاده از **Supabase** (PostgreSQL). Supabase در ایران دسترسی بهتری دارد و به راحتی با Cloudflare قابل Proxy شدن است.
*   **Authentication:** استفاده از **Supabase Auth**.
*   **API Gateway (اختیاری اما توصیه شده):** استفاده از **Cloudflare Workers** به عنوان یک لایه واسط برای دور زدن محدودیت‌های احتمالی و مدیریت منطق حساس سمت سرور.
*   **Storage:** استفاده از **Supabase Storage**.

---

## ۴. نقشه مهاجرت مرحله‌به‌مرحله (Migration Roadmap)

### مرحله ۱: راه‌اندازی زیرساخت Supabase
*   ایجاد پروژه در Supabase.
*   طراحی جداول دیتابیس (Courses, Orders, Users, Settings, Gallery) بر اساس ساختار فعلی Firestore.
*   تنظیم Storage Buckets و Policyهای دسترسی (RLS).
*   **سختی:** متوسط | **زمان تخمینی:** ۱ روز

### مرحله ۲: پیاده‌سازی لایه واسط (Cloudflare Worker)
*   ایجاد یک Worker برای پروکسی کردن درخواست‌های Supabase.
*   این مرحله تضمین می‌کند که حتی در صورت فیلتر شدن دامنه‌های Supabase، سایت همچنان کار می‌کند.
*   **سختی:** متوسط | **زمان تخمینی:** ۱ روز

### مرحله ۳: جایگزینی Authentication
*   تغییر توابع لاگین و ثبت‌نام در `user/js/auth.js` و `admin/js/login.js`.
*   به‌روزرسانی `js/user-auth-state.js` برای هماهنگی با Supabase Client.
*   **سختی:** بالا | **زمان تخمینی:** ۲ روز

### مرحله ۴: انتقال داده‌ها و تغییر توابع دیتابیس
*   جایگزینی کوئری‌های Firestore با کوئری‌های Supabase در `admin/js/admin.js` و سایر فایل‌ها.
*   نوشتن اسکریپت برای انتقال داده‌های موجود (در صورت وجود داده مهم).
*   **سختی:** بالا | **زمان تخمینی:** ۲-۳ روز

### مرحله ۵: انتقال Storage
*   تغییر کدهای آپلود و نمایش تصویر در بخش گالری و دوره‌ها.
*   **سختی:** متوسط | **زمان تخمینی:** ۱ روز

---

## ۵. تحلیل ریسک (Risk Analysis)

1.  **بروز اختلال در زمان انتقال:** با استفاده از یک Parallel Setup (نگه داشتن Firebase در کنار Supabase در طول مرحله توسعه)، ریسک از کار افتادن سایت به صفر می‌رسد.
2.  **تغییر مدل داده:** از NoSQL به Relational نیاز به دقت در طراحی جداول دارد تا کوئری‌ها بهینه باشند.
3.  **امنیت:** باید سیاست‌های Row Level Security (RLS) در Supabase به دقت تنظیم شوند تا داده‌های کاربران محافظت شود.

---

**منتظر تأیید شما برای شروع مرحله ۱ (طراحی دیتابیس در Supabase) هستم.**
